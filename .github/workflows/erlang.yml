name: Erlang CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    container:
      image: erlang:21.3.8.2

    steps:
    - uses: actions/checkout@v1
      
    - name: Compile
      run: make compile
      
    - name: Release
      run: make release
